/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * 
 * Browser Support:
 * - Modern evergreen browsers
 * - Safari >= 10
 * - Chrome >= 55
 * - Edge >= 13
 * - iOS 10 and Chrome mobile
 */

/**
 * Zone.JS - Required by Angular
 * Disable specific features by setting flags before zone.js load:
 * 
 * Examples:
 * (window as any).__Zone_disable_requestAnimationFrame = true;
 * (window as any).__Zone_disable_on_property = true;
 * (window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove'];
 */
import 'zone.js';

/**
 * Angular Localization
 * Required for ng-bootstrap and i18n features
 */
import '@angular/localize/init';

/**
 * Application-specific polyfills
 * Add any custom polyfills needed by the application here
 */
