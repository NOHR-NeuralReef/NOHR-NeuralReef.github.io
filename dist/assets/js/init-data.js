// Initialize data structure
window.data = window.data || {
    identity: {},
    quote: {},
    NavBar: { links: [] }
};

// Log initialization
console.log('Data structure initialized:', window.data); 